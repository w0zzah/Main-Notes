#include <stdio.h>
#include <stdint.h>

int main(void)
{
    int32_t firstNumber;
    int32_t secondNumber;
    scanf("%d %d", &firstNumber, &secondNumber);
    printf("%d", firstNumber + secondNumber);
}